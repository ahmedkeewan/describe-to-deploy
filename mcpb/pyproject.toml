[project]
name = "service-buddy-mcpb"
version = "0.1.0"
description = "Service Buddy's MCP server, packaged as a Claude Desktop extension (.mcpb)."
requires-python = ">=3.10"
dependencies = [
    "mcp>=2.1.1,<3",
]
