harness/.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Runtime state written by the harness, not source
stack-plan.json
stack-state.json
events.jsonl
environments.json
environment-snapshots.json
*.lock

# Local env
.env
.env.local

# Written by setup.sh -- machine-specific absolute paths, not portable source
.mcp.json
.cursor/mcp.json

# Built by mcpb/build.sh -- staging copy + packed output, not committed source
mcpb/.build/
mcpb/*.mcpb
